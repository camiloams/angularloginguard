import { InMemoryDataServices } from './in-memory-data-services';

describe('InMemoryDataServices', () => {
  it('should create an instance', () => {
    expect(new InMemoryDataServices()).toBeTruthy();
  });
});
